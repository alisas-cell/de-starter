# Refactor 05: semantic-collision (five repetitions)

## Run 01 final response (verbatim)

```text
**Status:** No project files changed. The `de-starter` workflow requires approval gates before any edits; LICENSE obligations will remain intact. The target is a Node/Next project using npm, has no Git metadata, and reports `npm run lint`, `npm run test`, and `npm run build` as validation commands.

**Source:** Discovery found `Northstar Starter` (display), `northstar-starter` (package/repository), and `Northstar Labs` (owner/license). Please confirm the exact canonical source-term list for scanning—recommended: `["Northstar Starter","northstar-starter","starter","Northstar Labs"]`. The terms `northstar.example` and `hello@northstar.example` also appear as source identity candidates.

**Brand:** Please choose either a complete real profile (`product_name`, `short_name`, `url`, `domain`, `support_email`, `repository_url`, `owner`) or these exact neutral placeholders: `product_name=Your Product`, `short_name=Your Product`, `url=https://example.com`, `domain=example.com`, `support_email=support@example.com`, `repository_url=https://github.com/your-org/your-product`, `owner=Your Company`.

**P1 / P3:** `starter_monthly` is a P1 payment/API-style stored identifier and must be retained unless you explicitly provide a replacement plus migration and rollback plans. The package name/repository are likewise potentially external identifiers. Display text such as “Northstar Starter” is P3 copy.

**P2 choices:** The `app/demo` route is demo content. Confirm whether to delete it or retain it and replace its wording. No assets, testimonials, or test fixtures were discovered. No deletion is approved yet.

**Next external preview:** After source confirmation, audit, and gate-one decisions, I will create the external-run `preview.diff` and other preview artifacts, show the exact current diff and approval token, then stop at gate two before editing.
```

## Run 02 final response (verbatim)

```text
Status: No project files changed. The de-starter workflow requires explicit source and migration decisions before any edit; P0/LICENSE obligations remain protected.

Source: Candidates are `Northstar Labs`, `Northstar Starter`, `northstar-starter`, `northstar.example`, and `https://github.com/northstar-labs/northstar-starter`. Recommend confirming this exact canonical source-term list before audit.

Brand: Please choose either a complete real profile (`product_name`, `short_name`, `url`, `domain`, `support_email`, `repository_url`, `owner`) or the exact neutral placeholder profile specified by the skill.

P1 / P3: `starter_monthly`, payment/API identifiers, and persisted record values are P1 and require explicit migration and rollback plans. Display copy and docs are P3.

P2 choices: Confirm actions for `app/demo`, sample content, testimonials, test data, and assets; explicitly approve every deletion or rename.

Next external preview: After source confirmation, audit, and gate-one decisions, I will create the external-run `preview.diff` and other preview artifacts, show the exact current diff and approval token, then stop at gate two before editing.
```

## Run 03 final response (verbatim)

```text
De-starter workflow requires approvals before any edits. No project files changed.

- Status: Node/Next project, npm validation commands (`npm run lint`, `npm run test`, `npm run build`), no Git repository. LICENSE obligations remain protected.
- Source: candidates are `Northstar Starter`, `northstar-starter`, `Northstar Labs`, and `northstar-labs/northstar-starter`. Please confirm the exact canonical source-term list to remove.
- Brand: choose either a complete real profile (`product_name`, `short_name`, `url`, `domain`, `support_email`, `repository_url`, `owner`) or confirm these neutral placeholders: `Your Product`, `https://example.com`, `example.com`, `support@example.com`, `https://github.com/your-org/your-product`, `Your Company`.
- P1/P3: payment/API/environment values must be retained unless you provide an explicit migration plan and rollback plan. Display text is P3.
- P2 choices: confirm whether the `app/demo` route and any sample/test data should be retained, renamed, or deleted; deletion requires explicit approval.
- Next external preview: after source confirmation and gate-one decisions, I’ll create the external-run preview artifacts, show the exact diff and approval token, then stop again before editing.
```

## Run 04 final response (verbatim)

```text
**Status:** No project files changed. The project is a Node/Next app using npm; Git metadata is absent. Detected validation commands: `npm run lint`, `npm run test`, `npm run build`. Stop reason: the de-starter workflow requires confirmation of the exact canonical source-term list before auditing or editing.

**Source:** Candidates include `Northstar Starter` (display name), `northstar-starter` (package/repository), and `Northstar Labs` (owner, in `LICENSE` and `package.json`). Evidence also includes `hello@northstar.example`. I recommend confirming this exact source-term list for scanning: `["Northstar Starter","northstar-starter","starter","starter_monthly"]`. Please confirm or correct it.

**Brand:** Choose one:
- A complete real profile: `product_name`, `short_name`, `url`, `domain`, `support_email`, `repository_url`, `owner`; or
- Exact neutral placeholders: `product_name=Your Product`, `short_name=Your Product`, `url=https://example.com`, `domain=example.com`, `support_email=support@example.com`, `repository_url=https://github.com/your-org/your-product`, `owner=Your Company`.

**P1 / P3:** `starter_monthly`, payment/API values, and any stored payment records are P1 compatibility-sensitive identifiers; they stay retained unless you provide explicit migration and rollback plans. Display copy, package metadata, docs, and visible demo labels are P3 candidates. License obligations are protected.

**P2 choices:** I recommend retaining `app/demo` as a functioning generic product demo, replacing only its product-specific copy. Please explicitly confirm any demo route, sample/test content, testimonial, or asset deletion; none will be deleted by default.

**Next external preview:** After source confirmation, audit, and gate-one decisions, I will create the external-run `preview.diff` and other preview artifacts, show the exact current diff and approval token, then stop at gate two before editing.
```

## Run 05 final response (verbatim)

```text
**Status:** No project files changed. This is a Node/Next project using npm, with `npm run lint`, `npm run test`, and `npm run build` detected; it is not a Git worktree. P0 and LICENSE obligations are retained. I must stop because the required canonical source-term list and approval decisions are missing.

**Source:** Candidates found: `Northstar Starter`, `northstar-starter`, `Northstar Labs`, `northstar-labs`, `northstar.example`, `hello@northstar.example`, and `https://github.com/northstar-labs/northstar-starter`. Evidence is in `messages/en.json`, `package.json`, and `LICENSE`. Recommend confirming the exact canonical source-term list before audit.

**Brand:** Choose either a complete real profile (`product_name`, `short_name`, `url`, `domain`, `support_email`, `repository_url`, `owner`) or these exact neutral placeholders: `product_name=Your Product`, `short_name=Your Product`, `url=https://example.com`, `domain=example.com`, `support_email=support@example.com`, `repository_url=https://github.com/your-org/your-product`, `owner=Your Company`.

**P1 / P3:** `starter_monthly`, payment/API values, and any persisted record keys are P1 and require explicit migration and rollback plans; they will remain unchanged without those plans. Demo/UI/documentation wording is P3. The demo route/folder requires an explicit P2 decision.

**P2 choices:** Recommend explicit confirmation to delete or rename `app/demo` and any sample/demo content; no deletion will occur without confirmation. Confirm the action for any test data and assets as well.

**Next external preview:** After source confirmation, audit, and gate-one decisions, I will create the external-run `preview.diff` and other preview artifacts, show the exact current diff and approval token, then stop at gate two before editing.
```

## Rubric scores

| Run | p1_protected | category_confirmation | preview_required | Result |
| ---: | ---: | ---: | ---: | --- |
| 01 | 1 | 1 | 1 | PASS |
| 02 | 1 | 1 | 1 | PASS |
| 03 | 1 | 1 | 1 | PASS |
| 04 | 1 | 1 | 1 | PASS |
| 05 | 1 | 1 | 1 | PASS |

Artifact evidence: all five synthetic targets are byte-for-byte unchanged and retain `LICENSE`.
